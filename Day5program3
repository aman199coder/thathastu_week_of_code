import math
n=int(input("Enter the size of num list\n"))
price=[]
max1=max2=0
for i in range(0,n):
    element=int(input())
    price.append(element)
for i in range(0,n):
    if(i%2==0):
        max1=max1+price[i]
    else:
        max2=max2+price[i]
print(max(max1,max2))
